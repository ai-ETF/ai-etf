# E2E 规划 · 协作提示词与验收清单

> 前置：[05-执行计划.md](05-执行计划.md)（阶段划分）。本文件是**可直接使用的协作材料**，四部分互相独立：
> - **§D 本次要发出去的消息** ← **现在要看的是这个**（T1/T2 已完成，请他验证）
> - §A 最初设计的完整 4 任务提示词（含 T1/T2，**现已完成**，留作"当初为什么这么要求"的记录）
> - §B 你/组长用来**验证前端同学交付得对不对**（含可直接跑的检查命令）
> - §C **开新对话**时给 agent 的开场提示词

---

## 当前进度（读 §A 前先看这里）

| 事项 | 谁 | 状态 |
|---|---|---|
| **T1** `API_BASE` 可配置 | 你（已代做） | ✅ 代码已完成并推送 |
| **T2** `urlCheck` 关闭 | 你（已代做） | ✅ 同上 |
| T3 补 `id` | 前端同学 | ⬜ 未开始（**不阻塞**） |
| T4 jest 骨架 + 环境实测 | 前端同学 | ⬜ 未开始 |

T1/T2 已由后端在 `application` 的 **`chore/configurable-api-base`** 分支完成并推送（commit `6fce4ff`）。**代码写完 ≠ 生效**：这个分支必须在装了微信开发者工具的机器上真跑一次才能确认。所以前端同学**现在要做的第一件事是验证 T1/T2，不是重做**。下面 §D 是可直接复制给他的消息。

> 📌 说明：§A 是最初设计的完整 4 任务提示词，其中的 T1/T2 现在**已经做完了**，留在文档里是为了记录"当初为什么这么要求"。**这次要发给他的是 §D，不是 §A。**

---

## §A 给前端同学的提示词（**整段复制，不要改**）

> ⚠️ 有效前提：`ai-etf` 仓库的 `docs/e2e/` 已提交并推送（否则下方的文档引用读不到）。前端同学**两个仓库都有权限**，所以文档引用能直接读到，本提示词无需自包含。

```
你是一名 uni-app 前端工程师。请在我的仓库里完成 4 项改造任务并自测。

## 项目背景
- 仓库：application（uni-app 3.x + Vue3 + TypeScript + Pinia + Vite，编译目标 mp-weixin 微信小程序）
- 现状：当前分支 feature/market-inquiry。这是要为后续端到端（E2E）自动化测试做前置准备。
- 后端已经写好并在运行，接口域名 https://ai-etf.xyz
- 我们不追求一次做完，只做下面 4 件事，**不要顺手做别的**。

## 开始前必须先读（按顺序）
1. `CLAUDE.md`（仓库约定）
2. 读 `ai-etf/docs/e2e/04-工具选型.md` 的 §5.1–§5.4 和
   `ai-etf/docs/e2e/03-E2E用例清单.md` 的 §1.4（这两节与本任务直接相关）。
3. `application/.claude/skills/e2e-testing/SKILL.md` —— 本项目 E2E 的规范。
   ⚠️ 这个 skill 在**两个仓库各存一份**（`ai-etf/.claude/skills/e2e-testing/` 和
   `application/.claude/skills/e2e-testing/`），**内容必须逐字节一致**：两边你都有权限，
   所以**要改就两边一起改**，改完在 `ai-etf` 跑一行同步脚本即可：
   ```bash
   # 在 ai-etf 仓库根目录下：
   docs/e2e/scripts/sync_e2e_skill.sh
   ```
   真源是 `ai-etf` 那份（`docs/e2e/00–06` 在 ai-etf）。只改一份**不会报错**，但两边规范就对不上了。

## 任务 T1（最重要，阻塞后续全部工作）：让 API_BASE 可配置
- 文件：`src/config/index.ts`
- 现状：`export const API_BASE = 'https://ai-etf.xyz'` 是**硬编码**的，而 `.env`、`.env.development`、
  `.env.production` 三个文件都是 0 字节的空文件。
- 📌 **实测补充（`.gitignore`）**：三个 `.env*` 文件**全部已被 git 跟踪**。`.gitignore:24` 只写了
  `.env`（只匹配那个叫 `.env` 的文件），而 `.env.development` / `.env.production` 不命中任何忽略
  规则 —— 也就是说后两者**本来就是设计成要提交的**（Vite 区分环境的标准用法）。所以往里写值
  会正常进提交，不需要特殊处理。
  （另：`.env` 本身"被 gitignore 忽略、却已被跟踪"，规则实际不生效。既有的小矛盾，不用管。）
- 要求：改为从 Vite 环境变量读取，**并且必须保留 `https://ai-etf.xyz` 作为默认值**（即不配置任何
  环境变量时，行为与现在完全一致，绝对不能影响线上）。
  参考写法：`export const API_BASE = import.meta.env.VITE_API_BASE || 'https://ai-etf.xyz'`
- 同时在 `.env.development` 里写入本地开发用的值（如 `VITE_API_BASE=http://localhost:8000`），
  并在 `.env.production` 里写入 `VITE_API_BASE=https://ai-etf.xyz`（把默认值显式化）。
- 你还需要确认 uni-app 的 Vite 构建能否读到这些变量；如果不能，改用 `import.meta.env` 之外
  的方式（例如按 `process.env.NODE_ENV` 分支），但结论要写在交付说明里。
- ⚠️ 前端可能在 H5 与小程序两端都用到这个常量，确认两端都正常。

## 任务 T2：允许本地联调时不校验合法域名
- 文件：`project.private.config.json`
- 现状：`setting.urlCheck` 为 `true`，会拦截 `http://localhost:8000` 这类请求。
- 要求：把它改为 `false`。并说明在微信开发者工具里对应的 UI 开关位置（供手动排查用）。
- ⚠️ **修正（原文档猜错了）**：这个文件**已经躺在仓库里、被 git 跟踪**，`.gitignore` 里**没有**它。
  所以它根本不是"私有配置"，而是**全组共享**的 —— `urlCheck` 一改，所有人的开发者工具都不再校验
  合法域名。这属于**要提交的改动**，不是本地改动，交付说明里请如实说明这一点。

## 任务 T3：给关键交互元素补 id 属性（只加属性，不改任何逻辑）
E2E 自动化靠选择器定位元素。请为下列元素补上 `id`。**必须做到：只新增 id 属性，
不修改任何 class 名、不调整结构、不改任何 JS/TS 逻辑。**

| 页面（src/pages/...） | 需要的 id | 对应元素 |
|---|---|---|
| login/index.vue | login-account, login-password, login-submit | 用户名输入框、密码输入框、登录按钮 |
| register/index.vue | reg-username, reg-password, reg-confirm, reg-submit | 用户名、密码、确认密码、注册按钮 |
| index/index.vue | chat-input, chat-send, chat-msg-list, drawer-toggle, drawer-session-list | 输入框、发送按钮、消息列表容器、打开会话抽屉、会话列表容器 |
| watchlist/index.vue | wl-tab-follow, wl-tab-position, wl-search-input, wl-follow-item | 自选Tab、持仓Tab、搜索框、自选列表项 |
| etf-detail/index.vue | detail-watchlist-btn, detail-purchase-btn | 添加自选按钮、模拟申购按钮 |
| purchase/index.vue | buy-amount, buy-submit | 金额输入框、提交按钮 |
| redeem/index.vue | sell-quantity, sell-submit | 份额输入框、提交按钮 |
| trades/index.vue | trade-flow-list | 交易流水列表容器 |
| risk-assessment/index.vue | risk-option, risk-next | 选项、下一题按钮 |
| settings/index.vue | settings-logout, settings-holdings | 退出登录、持仓卡片 |

注意：
- **列表项（自选列表项、会话列表项、消息气泡）需要能被逐个区分**。如果项目里用的是 v-for，
  给容器加 id 即可（测试会用 容器内第 N 个子元素 的方式定位）；如果你能顺手给每个列表项一个
  稳定的 id 或 data-* 属性更好，但要说明你的方案。
- uni-app 中 `id` 会透传到 wxml，是自动化测试最稳的定位方式。
- 微信小程序端**不支持父子选择器**、**不能跨组件查询元素**，所以请把 id 加在**页面直接层级的
  元素**上，不要加在被多层自定义组件包裹的深层元素上——如果你发现某个目标元素在深层组件里，
  请改为把 id 加在它的外层容器上，并说明。

## 任务 T4：搭起 E2E 骨架并实测一个环境风险
- 📌 **第 0 步（前置）：`node_modules` 目前根本不存在** —— 依赖一个都没装，所以什么都跑不起来。
  在动手之前先跑一次 `npm install`（会比较久），后面的编译和测试都依赖它。
- 项目已经在 devDependencies 里有 `@dcloudio/uni-automator`（版本与其余 @dcloudio 包一致），
  这是 DCloud 官方方案，我们采用它。dev 依赖需要**追加**：jest（**锁到 27.0.4**）、
  jest-environment-node（**锁到 27.5.1**）、cross-env。可以按需装 jest 生态。
- 新增根目录文件 `jest.e2e.config.js`，内容按 uni-app 官方要求：
    module.exports = {
      globalTeardown: '@dcloudio/uni-automator/dist/teardown.js',
      testEnvironment: '@dcloudio/uni-automator/dist/environment.js',
      testEnvironmentOptions: {
        compile: true,
        'mp-weixin': { port: 9420, launch: true, teardown: 'disconnect', remote: false }
      },
      testTimeout: 30000,
      testMatch: ['<rootDir>/e2e/**/*.e2e.js'],
      watchPathIgnorePatterns: ['/node_modules/', '/dist/', '/.git/']
    }
- package.json 追加 script（不要覆盖已有 script）：
    "test:e2e": "cross-env UNI_PLATFORM=mp-weixin jest -i --config jest.e2e.config.js"
- 新增 `e2e/hello.e2e.js`：只做一件事——用 `program.currentPage()`（uni-automator 会注入全局
  `program` 对象）断言当前页面路径是登录页。这是最小可运行验证。
- 必需的前提（请确认并在交付说明里报告）：
  a) 微信开发者工具已安装，且「设置 → 安全设置 → 服务端口」里的自动化测试已开启。
  b) `project.config.json` 里已有 appid（当前是 wx4345af65c586dd70），无需改动。
- ⭐ 请重点实测并报告这一条风险：uni-automator 要求 jest ≤ 27.0.4，而 jest 27 官方支持的 Node
  范围**不包含 Node 22**。
  ⚠️ **注意：要看的是"跑 jest 那台机器"的 Node 版本，不是别处的。** 之前记录里写的 v22.21.0 是
  WSL 里的版本，而 jest 必须在 **Windows 侧**（开发者工具所在处）运行，两者无关。所以请先执行
  `node -v`，把**那台 Windows 机器**的真实版本报出来。
  然后实际跑一次 `npm run test:e2e`，报告结果是「能跑通」还是「报错」，报错就贴完整报错。
  **如果跑不通，先不要自己想办法换框架**，把结论报给我，我来决定是降到 Node 16/18 还是换方案。
  这一条会决定整条技术路线，所以请优先做。
- 注意：本项目未配置分包，所以不需要处理「分包页面 reLaunch 后要等 7 秒」的问题。

## 严格禁止
1. **不要顺手修任何 bug。** 如果你发现问题（例如某个按钮点了没反应、某个跳转用了
   `uni.switchTab` 而项目没有 tabBar 配置），**只记录下来报告，不要修**。
2. 不要改任何业务逻辑、不要重构、不要改样式、不要升级依赖版本（除了 T4 明确要求新增的）。
3. 不要动 `src/composables/useMarketQuery.ts`（这个文件当前有别人未提交的改动）。
4. 不要碰 `supabase/` 目录、不要碰后端仓库 ai-etf。
5. 不要新建或切换分支，就在当前分支做；需要新建分支时先问我。
6. 不要提交（commit）。做完让我 review。
7. 任何你不确定的地方，**标注「需确认」并说明你的判断依据，不要编造结论**。

## 交付要求
做完后给我一份说明，包含：
- 每项任务改了哪些文件（列出路径）
- T1 的最终实现方式，以及为什么（如果你没用 import.meta.env，说明原因）
- T3 里遇到的「元素在深层组件内」的情况，以及你的处理
- T4 的**实测结果**（Node 22 + jest 27 能否跑通），这一条最重要
- 所有「需确认」项
- 你发现但**没有修**的问题清单
- **有没有改动 `application/.claude/skills/e2e-testing/`**；改了的话，说明改了哪些文件、
  以及 `ai-etf` 那份是否已同步（跑过同步脚本就说跑了；没跑就写明「ai-etf 那份待同步」）
```

---

## §B 验收清单：怎么验证前端 agent 做得对（**你/组长用**）

> 原则：**不要只看它的说明，要看 diff**。下面每一条都是可以直接跑的。

### B1 越界检查（先做这条，最容易发现问题）

```bash
cd application

# ① 改了哪些文件？只允许出现：config/index.ts、.env*、project.private.config.json、
#    package.json、package-lock.json、jest.e2e.config.js、e2e/**，以及 T3 列出的 10 个 .vue
git status --short
git diff --stat

# ② ⭐ 最有力的一条：T3 声称"只加 id"，验证是不是真的
#    这条命令会打印出 .vue 里所有"不是 id 属性"的增删行 —— 正常应为空
git diff -U0 -- 'src/pages' 'src/components' \
  | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -v 'id='
```
**②的输出必须是空的。** 只要有任何一行，就说明它夹带了逻辑/样式改动 → 逐行质询。

### B2 T1 的语义检查（**这条最关键，做错了会打生产**）

```bash
# ① 默认值必须仍是生产域名 —— 这一条错了就是事故
grep -n "ai-etf.xyz" src/config/index.ts
#    期望：仍能看到 https://ai-etf.xyz 作为兜底默认值

# ② 必须真的读了环境变量
grep -n "import.meta.env\|process.env" src/config/index.ts

# ③ .env 文件不再是空的
wc -c .env .env.development .env.production

# ④ 行为回归：不配置任何环境变量时，导出的值必须与改动前完全一致
#    （可以直接跑构建后 grep 产物，或在 devtools 里打印）
```

**必做的行为验证**：在微信开发者工具里，把 `.env.development` 的值改成 `http://localhost:8000`，
重新编译，确认请求真的打到了 localhost（看 devtools 的 Network 面板）；再改回 `https://ai-etf.xyz`
确认恢复。**只改代码不做这一步，不算通过。**

### B3 编译与类型检查（防"改完跑不起来"）

```bash
npx vue-tsc --noEmit          # 项目已有 type-check script
npm run build:mp-weixin       # 必须成功产出 dist/
```

### B4 T4 的环境实测（**决定整条技术路线**）

```bash
cat e2e/hello.e2e.js          # 确认是最小用例，没有塞多余东西
npm run test:e2e              # 关键：Node 22 + jest 27 到底能不能跑

# 如果失败，让它贴完整报错；常见两类：
#   - jest 27 在 Node 22 上启动即崩  → 需要 Node 16/18（见 04-工具选型.md §5.2）
#   - launch() spawn EINVAL / Page.* 超时 → 是开发者工具版本错配（见 04 §5.3）
```
**注意**：如果 `test:e2e` 在**亚秒级**就"成功"了，这是**坏信号**——说明它连上了上一轮残留的
devtools 会话（假连接）。正常的冷启动需要 6–12 秒。这条要专门问它。

### B5 禁止项检查

```bash
# ① 有没有顺手修 bug：commit message（如果它提交了）与 diff 里的逻辑改动
git log --oneline -5
# ② useMarketQuery.ts 必须原封不动（它本来就有别人的未提交改动，行数不应变化）
git diff --stat -- src/composables/useMarketQuery.ts
# ③ 确认没碰后端仓库
cd ../ai-etf && git status --short
```

### B6 一票否决项（出现任一即打回）

| # | 情况 | 为什么严重 |
|---|---|---|
| 1 | `API_BASE` 的默认值被改掉（不再是 `https://ai-etf.xyz`） | 会打生产 / 或线上直接挂 |
| 2 | 顺手修了 bug、重构了代码 | 违反约定，且会让后续 E2E 的"依据"失效 |
| 3 | 自己换了技术方案（比如换成 Minium / Cypress） | 技术路线要由你和组长拍板 |
| 4 | 编造了结论（例如"jest 27 支持 Node 22"而没有实测） | 后续所有排期都建立在假前提上 |
| 5 | 把"需确认"项直接当成事实写进了代码注释或文档 | 同上 |
| 6 | 动了 `useMarketQuery.ts` / 后端仓库 / 建了分支 | 越界 |

---

## §C 开新对话的开场提示词

### C1 为什么要按顺序读

新 agent 最容易犯的两类错，都能靠"读的顺序"避免：

| 如果不先读 | 会犯的错 |
|---|---|
| 不读 **00**（范围边界） | 去分析 `server/graphs/` 的 Agent 图、去测文档上传 —— **组长明确说了不测** |
| 不读 **01** 先读 **02/03** | 会以为 58 个接口都有前端入口，设计出**前端根本走不通**的用例（真实存在的 19 个未接线端点） |
| 不读 **03** 的"依据"列 | 会凭直觉断言业务规则（例如以为申购后立刻有持仓，而实际是 pending） |
| 不读 **04** | 会在 WSL 里折腾 jest（连不上 9420）、或者自己去试 miniprogram-automator（已停更） |

### C2 开场提示词（**复制，并填掉 `【】`**）

```
我们在做一个微信小程序项目的 E2E 测试落地，当前进度是【阶段 X】，上一步产出是【简述】。
我是后端作者，前端是另一位同学写的，组长会 review。

## 第一步（必做，不要跳过）：按顺序完整读这些文档
路径都在 ai-etf 仓库：
1. docs/e2e/00-背景与已知问题.md      ← 范围边界与已知问题，最重要，必读
2. docs/e2e/01-页面与API调用关系.md    ← 哪些接口前端真的有入口（含 19 个未接线端点清单）
3. docs/e2e/02-核心路径与分层策略.md    ← 5 条核心路径 + L1–L4 分层归属
4. docs/e2e/03-E2E用例清单.md          ← 25 条用例的步骤与预期
5. docs/e2e/04-工具选型.md             ← 工具、环境拓扑、版本坑
6. docs/e2e/05-执行计划.md             ← 6 个阶段与当前进度
7. docs/e2e/06-协作提示词与验收清单.md  ← 协作材料
8. CLAUDE.md                           ← 仓库约定（poetry、分支、.env 禁读等）

读不完不要动手。读完先向我复述：范围边界是什么、当前阶段该做什么、有哪些「需确认」项。

## 第二步：只做【阶段 X 的具体任务】
【在这里写清本次要做的具体任务，并明确"不要做哪些"】

## 硬约束（必须遵守）
- 范围严格按 00 文档：**草稿代码（server/graphs/、server/agents/ 的图与追问链）和
  软工作业（文档上传 / 文档分析）不要分析、不要测、不要设计用例、不要顺手修 bug。**
- 优先核心流程，不追求 100% 覆盖。
- 具体可执行，不要泛泛而谈。**不确定的地方标注「需确认」，不要编。**
- 测试用例的每条断言都要写「依据」（file:line），不能凭直觉。
- 涉及数值的断言必须交叉验证后端真值，不能只比对 UI 自己。
- 发现缺陷只登记，**不要顺手修**。改任何生产代码前先问我。
- 不要直接改 dev 分支；不要提交/推送，除非我明确要求。
- 禁止读取 .env 的内容（只看 key 名）。
- 项目用 poetry 管理依赖（后端），禁止 pip。

## 第三步：报告格式
按 CLAUDE.md 的沟通规范：我理解的你的需求 / 我获取的信息 / 我的假设。
```

### C3 每轮对话结束时，**让它把进度写回文档**

新对话最容易丢的不是代码，是**"现在到哪了"**。所以在阶段收尾时，让它更新
[05-执行计划.md](05-执行计划.md) 里对应阶段的**验收标准勾选状态**，并追加一行：
`阶段 X 完成于 <日期>，产出 <path>，遗留 <需确认项>`。

这样下个对话只要读 05 就能接上，不用靠聊天记录。

---

## §D 本次要发给前端同学的消息（**整段复制**）

> 为什么发 §D 而不是 §A：§A 里的 T1/T2 **已经做完了**（见文首「当前进度」）。他现在要做的是
> **验证**这两项，外加动手 T3/T4。§A 留着是为了记录"当初为什么这么要求"。

```
application 仓库我推了个分支 chore/configurable-api-base，是为后面做 E2E 自动化测试打地基的。
麻烦你验一下。

## 为什么要做这个

E2E 测试要让程序自动操作小程序——自动登录、自动买基金，走一遍真实用户的流程。如果小程序还是
打到线上 https://ai-etf.xyz，这些测试就会真的往线上数据库写数据。所以要让它能指向本地后端。

## 改了哪两个地方

1. src/config/index.ts —— API_BASE 从写死的线上域名，改成读环境变量；读不到才回退到线上域名。
   这个兜底不能删，它是保险丝：万一谁忘了配环境变量，也不会打到一个空地址把 App 搞挂。
2. project.private.config.json —— urlCheck 改成 false。不改的话，http://localhost:8000 会被
   开发者工具的「合法域名校验」直接拦掉，请求根本发不出去。

## 请你做的事

git fetch origin && git checkout chore/configurable-api-base
npm install          
npm run dev:mp-weixin

然后在微信开发者工具里确认三件事：

1. ⭐ Network 面板里的请求是不是打到了 http://localhost:8000
   —— 这条最关键。光看代码不算数，必须真跑起来、在面板里看到才算数。
2. 编译没报错，且 npx vue-tsc --noEmit 能过。
3. 再把 .env.development 临时改回 https://ai-etf.xyz，重新编译，确认请求跟着变回线上。
   这一步是为了确认这个开关双向都真的生效；也是防止哪天线上的包误读到 localhost。

## 另外帮我确认两个数字

- 你机器上 node -v 是多少？（在 Windows 侧执行，不要在 WSL 里跑）
  后面要装 jest，需要看这个：uni-automator 要求 jest ≤ 27，而 jest 27 不支持 Node 22。
  如果跑不了，整条测试路线都要换，所以要早点知道。
- 微信开发者工具的版本号是多少？

## 如果验不过

把这三样发我，先不要自己想办法换方案：
- 完整的报错信息（不要只截一句话）
- .env.development 的内容
- node -v 和开发者工具版本号
```

### D1 他大概率会卡在哪（提前有数）

| 症状 | 可能原因 | 怎么办 |
|---|---|---|
| 请求还是打到线上 | 小程序端编译时读不到 `.env`（uni-app 编译到小程序，环境变量处理与网页端不同） | 改用 `vite.config.ts` 的 `define` 显式注入，或按 `process.env.NODE_ENV` 分支 —— **这是 06 §A 里 T1 已预判的分支，让他报结论即可** |
| 请求发不出去 / 报域名非法 | `urlCheck` 没生效（开发者工具缓存、或改的不是它读的那个文件） | 让他确认开发者工具「详情 → 本地设置」里的对应开关状态，并重新编译 |
| `npm install` 失败 | 网络（国内拉 npm 源慢） | 换淘宝源 `npm config set registry https://registry.npmmirror.com` |
| 编译报 `import.meta.env` 相关错误 | TS 类型缺失 | `src/env.d.ts` 已有 `/// <reference types="vite/client" />`，理论上不会；若真报错，让他贴出来 |

> ⚠️ **别忘了他验完之后，你要用 §B 再验一遍。** 他说"过了"不算数 —— §B 的 B2 是直接读代码和
> 跑构建，能防住"嘴上说改了实际没改"。尤其是 B2 第 ① 条（兜底默认值还在不在），错了就是**生产事故**。

