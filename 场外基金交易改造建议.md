# 场外基金交易改造建议

## 一、总体方向

1. **砍掉场内 ETF 交易逻辑** — ETF 交易涉及竞价、盘口、滑点、涨跌停等复杂机制，模拟盘无法真实还原，且代码量和复杂度远高于场外基金
2. **只做场外基金** — 宁可只支持少量基金交易，不可存在错误的交易逻辑。

---

## 二、可交易基金白名单

### 保留

- 指数基金（被动跟踪型，含 ETF 联接基金）
- LOF（上市型开放式基金）--> 可以暂时不做

### 排除

- 主动管理型基金
- 指数增强基金
- 混合基金
- 债券基金
- 货币基金

### 实现方式

建议实现方式：

1. 通过基金代码前缀来判断是否是可交易基金：适用情况为能够通过API等方式获取基金交易规则，也就意味着只要是符合该代码前缀的基金你都指导明确的费率。
2. 将可交易基金代码存到一个哈希表里，检索不到代码则返回`暂不支持该基金交易`。



---

## 三、费率规则改造

### 3.1 取消默认费率规则

**当前问题**：当 `fund_fee_rules` 表查不到该基金时，`_default_rule()` 会用硬编码的费率兜底，这是不可接受的——交易规则不能靠猜。

**要求**：查不到规则直接返回"不支持交易"，不走任何默认值。

### 3.2 赎回费档位应结构化存储

**当前问题**：赎回费档位硬编码为 4 档：

```python
if hold_days < 7:     rate = 1.5%
elif hold_days < 30:  rate = 1.0%
elif hold_days < 180: rate = 0.5%
else:                 rate = 0%
```

**问题**：不同基金赎回费结构差异大，有些 30 天免、有些 730 天才免，一刀切会算错。

**建议**：`fund_fee_rules` 表中使用 JSON 字段存储档位：

```json
{
  "redemption_fee_tiers": [
    {"days": 7, "rate": 0.015},
    {"days": 30, "rate": 0.010},
    {"days": 180, "rate": 0.005},
    {"days": 365, "rate": 0.0}
  ]
}
```

计算赎回费时遍历匹配对应档位。

---

## 四、按金额申购（关键逻辑修正）

**当前问题**：`buy()` 方法参数是 `quantity`（份额），场外基金实际是"买 X 元"而不是"买 X 份"：

```python
# 当前（错误）
def buy(self, user_id, fund_code, quantity, price=None)
```

**建议**：

- 有些基金支持 0 元起购，有些基金支持10元起购，有些基金限购10元。
- 我们可以简化为所有基金10元起购，但是限购的必须限购。

```python
# 修正（按金额申购）
def buy(self, user_id, fund_code, amount, price=None)
# amount: 申购金额（元）
# 实际份额 = (amount - 申购费) / 确认日净值，确认后才知道精确份额
```

---

## 五、Pending 期间资金冻结而非立即建仓

**当前问题**：15:00 后提交的场外申购，status 虽然标记为 pending，但实际上已经扣款、建仓、更新持仓了。这导致用户在份额确认前就能卖出未真正持有的基金。

**正确做法**：

| 阶段 | 资金 | 持仓 |
|------|------|------|
| 提交时（pending） | 冻结（不可用，不可取） | 不建仓 |
| 确认份额时 | 正式扣款 | 按实际份额建仓 |
| 确认失败 | 解冻回账户 | 无变化 |

这需要新增一张 `trade_orders` 或 `pending_orders` 表来记录 pending 状态，或者扩展已有的 `trade_orders` 表结构。

---

## 六、T+N 确认机制区分

**当前问题**：只有一个 `confirm_date`，没有区分不同基金类型的确认周期。

**实际场外基金确认规则**：

| 基金类型 | 申购确认日 | 可赎回最早 | 赎回款到账 |
|----------|-----------|-----------|-----------|
| 普通场外基金 | T+1 | T+2 | T+3~T+4 |
| ETF 联接基金 | T+1 | T+2 | T+3~T+4 |
| QDII 基金 | T+2 | T+3 | T+5~T+7 |
| 货币基金 | T+1 | T+1 | T+1~T+2 |

**建议**：在 `fund_fee_rules` 表中增加字段：

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `confirm_delay` | 申购确认延迟天数 | 1（T+1） |
| `redemption_settle_delay` | 赎回到账延迟天数 | 3（T+3） |

建议：

- 初期只做普通场外基金和 ETF 联接基金（两者都是 T+1 确认），后续可扩展 QDII。

