# 场外基金交易系统 — 当前问题

## 问题一：同一请求内 `get_fee_rule` 重复远程查询 3 次

**位置**：`server/services/fund_fee_service.py:43` + `server/services/portfolio_service.py`

**现象**：一次申购（`apply_purchase`）流程中，`get_fee_rule(fund_code)` 被调用 3 次：

```
apply_purchase()
  ├─ fee_svc.get_fee_rule(fund_code)          # 第1次：白名单校验 (L208)
  ├─ self._calc_purchase_fee()
  │    └─ svc.calc_purchase_fee()
  │         └─ self.get_fee_rule(fund_code)    # 第2次：算申购费 (L87)
  └─ self._get_fund_name()
       └─ svc.get_fund_name()
            └─ self.get_fee_rule(fund_code)    # 第3次：取基金名称 (L169)
```

赎回流程（`apply_redeem`）同理 3 次。确认流程（`_confirm_one_order`）中 buy 分支 2 次、sell 分支 2 次。

**问题本质**：每次 `get_fee_rule` 都是一次完整的 Supabase HTTP 远程调用（~25-100ms），同一只基金、同一次请求、同一时刻的费率规则不可能变化，但代码没有任何去重或缓存机制，白白多出 2 次网络往返。

---

## 问题二：赎回费档位匹配逻辑与 seed 数据语义不一致

**位置**：`server/services/fund_fee_service.py:134-140` + `sql/fund_fee_rules_seed.sql`

**现象**：`calc_redemption_fee` 的匹配逻辑是：

```python
# 找到第一个 days > hold_days 的档位
for tier in tiers_sorted:
    if hold_days < tier["days"]:
        rate = tier["rate"]
        break
```

即 `hold_days < tier.days` 时命中该档。但 seed 数据中大量基金使用了 `{"days": 9999, "rate": 0.0000}` 作为"兜底免费档"。

**真实基金的赎回费规则是区间制**，例如易方达沪深300ETF联接A的官方合同：

| 持有天数 | 赎回费率 |
|---------|---------|
| < 7天 | 1.50% |
| 7天 ~ 365天 | 0.50% |
| 365天 ~ 730天 | 0.25% |
| ≥ 730天 | 0.00% |

**具体问题**：

1. **`9999` 是硬编码的魔法数字**，不是真实档位边界。真实基金合同写的是"≥730天免费"，不是"≥9999天免费"。用 9999 做兜底意味着持有 730~9998 天的用户匹配到的档位描述与合同不符。

2. **档位语义丢失**：如果某只基金的真实规则是"持有 ≥30天免费"（如部分C类基金），seed 数据写的是 `{"days":9999,"rate":0.0000}`，但实际持有 30 天时命中的是 9999 档而非 30 档——虽然费率结果恰好相同（都是 0），但无法区分"30天免费"和"9999天免费"。

3. **缺少显式边界语义**：当前 JSON 结构只有 `days` 一个边界值，无法区分 `< N` 和 `≤ N` 的语义差异。如果某基金规则是"持有 ≤7天收1.5%"（含第7天），当前 `<` 比较会把第7天漏到下一档。

4. **数据库中存在废弃的旧赎回费字段，与 `redemption_fee_tiers` 重复且数据不一致**：当前 `fund_fee_rules` 表中同时存在两套赎回费数据：

   - **旧字段**（固定 4 列）：`redemption_fee_rate_7d`、`redemption_fee_rate_30d`、`redemption_fee_rate_1y`、`redemption_fee_rate_over1y`
   - **新字段**（JSONB）：`redemption_fee_tiers`

   代码层面已改用 `redemption_fee_tiers`（`calc_redemption_fee` 读的是 `rule.get("redemption_fee_tiers")`），旧字段不再被任何代码引用。但旧字段未被删除，且数据内容与 `redemption_fee_tiers` 不一致：

   以 `001595`（天弘中证银行ETF联接C）为例：

   | | 旧字段 | `redemption_fee_tiers` |
   |---|---|---|
   | 7天 | `0.0150` | `{"days":7, "rate":0.015}` ✅ 一致 |
   | 30天 | `0.0075` | **无对应档** ❌ 该基金真实规则无30天档 |
   | 1年 | `0.0050` | **无对应档** ❌ 该基金真实规则无1年档 |
   | over1y | `0.0000` | `{"days":9999, "rate":0}` ✅ 一致 |

   旧字段的 30 天档和 1 年档是迁移脚本批量填充的固定值（所有基金都填了 `0.0075` 和 `0.0050`），并非各基金的真实费率。而 `redemption_fee_tiers` 中才是按真实合同填写的档位。两套数据并存，容易造成混淆，且旧字段中的错误数据一旦被误读会导致计算错误。

---

## 问题三：定时确认任务无兜底机制，服务器离线时订单永久挂起

**位置**：`server/services/spot_cache_scheduler.py:71-77`

**现象**：Pending 订单确认任务配置为 APScheduler CronTrigger：

```python
CronTrigger(hour=15, minute=37, day_of_week="mon-fri")
```

每个交易日 15:37 触发一次。APScheduler 是**纯内存调度器**，不持久化任务状态。

**具体问题**：

1. **服务器在 15:37 不在线**（宕机、重启、部署中、网络故障等），该交易日的确认任务直接跳过，所有 `confirm_date ≤ 当天` 的 pending 订单**永远不会被确认**，用户资金永久冻结在 `frozen_cash`。

2. **积压效应**：服务器在 15:37 之后才启动（如 16:00 重启完成），APScheduler 只会在下一个交易日的 15:37 触发，当天该确认的订单要等到明天——而明天又会有新的 pending 订单需要确认，造成积压滚雪球。

3. **无启动补偿检查**：`start_scheduler()` 启动时只做了行情缓存预热（`_warmup_cache`），没有检查是否有遗漏的 pending 订单需要立即确认。即使服务器在 15:38 启动（只晚了 1 分钟），也不会补执行。

4. **无手动兜底入口**：虽然 `/portfolio/confirm-pending` 接口存在，但在上述场景中需要人工发现并主动调用，缺乏自动化的兜底保障。

---

## 问题四：当前数据库表结构无法完整表达不同基金的赎回规则差异

**位置**：`sql/migrate_v2_off_exchange.sql:133-150` + `sql/fund_fee_rules_seed.sql`

**现象**：`fund_fee_rules` 表的 `redemption_fee_tiers` 字段为 JSONB，结构为 `[{"days": N, "rate": R}]`，配合 `calc_redemption_fee` 中的 `hold_days < tier.days` 匹配逻辑使用。看似灵活，但实际无法覆盖以下真实基金规则场景：

### 4.1 申购费也是分档的，当前只存单一费率

当前 `purchase_fee_rate` 是一个 `DECIMAL(6,4)` 单值字段，只存了"互联网渠道1折后、＜100万元档"的费率。但真实基金的申购费是**按金额分档**的：

| 申购金额 | 原始申购费率 |
|---------|------------|
| < 100万 | 1.20% |
| 100万 ~ 500万 | 0.80% |
| ≥ 500万 | 每笔 1000 元 |

当前表结构无法存储金额分档，只能存一个固定费率。如果用户申购 500 万，系统仍按 1.20% 打折后的 0.12% 收费，与真实规则不符。

### 4.2 赎回费档位边界语义不完整

当前 JSON 结构 `[{"days": N, "rate": R}]` 只有一个 `days` 边界值，配合代码中的 `<` 比较使用。但真实基金合同的档位边界存在两种语义：

- **"持有时间 < N"**：当前代码能正确处理
- **"持有时间 ≤ N"**（含边界当天）：当前代码无法表达

例如某基金合同写"持有期 ≤7日赎回费 1.5%"，意味着第 7 天仍收 1.5%，但当前 `hold_days < 7` 在第 7 天会跳到下一档，少收费用。
